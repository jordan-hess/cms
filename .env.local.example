NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
AUTH_SECRET=
SUPABASE_JWT_SECRET=

# Phase 0: Postgres connection string for the SIT target (see lib/db/client.ts).
# Shape: postgresql://<user>:<password>@<host>:5432/<dbname>?sslmode=<require|disable|...>
# `sslmode` is read straight from this URL — set it to match what the target server
# actually supports (the current SIT cluster has SSL intentionally disabled).
DATABASE_URL=
